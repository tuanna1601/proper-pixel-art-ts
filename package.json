{
  "name": "proper-pixel-art-ts",
  "author": "Tuan Nguyen <tuanna1601@gmail.com>",
  "version": "0.0.2",
  "description": "Node-first TypeScript port of KennethJAllen/proper-pixel-art.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tuanna1601/proper-pixel-art-ts.git"
  },
  "type": "module",
  "main": "./dist/node.js",
  "module": "./dist/node.js",
  "types": "./dist/node.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/node.d.ts",
      "import": "./dist/node.js",
      "default": "./dist/node.js"
    },
    "./browser": {
      "types": "./dist/browser.d.ts",
      "default": "./dist/browser.js"
    }
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && bun build ./src/browser.ts --outfile ./dist/browser.js --target browser --format esm",
    "test:node-import": "bun run build && node ./scripts/node-smoke.mjs",
    "test:unit": "vitest run",
    "test:parity": "bun run build && playwright test",
    "test:parity-node": "bun run build && node ./scripts/node-parity.mjs",
    "test": "bun run test:unit && bun run test:node-import",
    "prepublishOnly": "bun run build && bun run test:unit && bun run test:node-import"
  },
  "keywords": [
    "pixel-art",
    "nodejs",
    "opencv",
    "image-processing",
    "typescript"
  ],
  "optionalDependencies": {
    "@opencvjs/node": "4.12.0-release.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  }
}
